<?xml version="1.0" encoding="UTF-8"?>
<Configuration packages="com.rabit.api">
    <Properties>
        <Property name="log.path">logs</Property>
        <Property name="log.pattern">%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</Property>
    </Properties>

    <Appenders>
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${log.pattern}" />
        </Console>

        <!-- File Appender - All logs -->
        <RollingFile name="File" fileName="${log.path}/app.log" filePattern="${log.path}/app-%d{yyyy-MM-dd}-%i.log">
            <PatternLayout pattern="${log.pattern}" />
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true" />
                <SizeBasedTriggeringPolicy size="10MB" />
            </Policies>
            <DefaultRolloverStrategy max="30" />
        </RollingFile>

        <!-- File Appender - Error logs -->
        <RollingFile name="ErrorFile" fileName="${log.path}/error.log" filePattern="${log.path}/error-%d{yyyy-MM-dd}-%i.log">
            <PatternLayout pattern="${log.pattern}" />
            <Filters>
                <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY" />
            </Filters>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true" />
                <SizeBasedTriggeringPolicy size="10MB" />
            </Policies>
            <DefaultRolloverStrategy max="30" />
        </RollingFile>

        <!-- File Appender - Debug logs -->
        <RollingFile name="DebugFile" fileName="${log.path}/debug.log" filePattern="${log.path}/debug-%d{yyyy-MM-dd}-%i.log">
            <PatternLayout pattern="${log.pattern}" />
            <Filters>
                <ThresholdFilter level="DEBUG" onMatch="ACCEPT" onMismatch="DENY" />
            </Filters>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true" />
                <SizeBasedTriggeringPolicy size="10MB" />
            </Policies>
            <DefaultRolloverStrategy max="20" />
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Application loggers -->
        <Logger name="com.rabit.api" level="DEBUG" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="File" />
            <AppenderRef ref="ErrorFile" />
            <AppenderRef ref="DebugFile" />
        </Logger>

        <!-- Spring Framework loggers -->
        <Logger name="org.springframework" level="INFO" />
        <Logger name="org.springframework.security" level="DEBUG" />
        <Logger name="org.springframework.web" level="INFO" />

        <!-- Hibernate loggers -->
        <Logger name="org.hibernate" level="INFO" />
        <Logger name="org.hibernate.SQL" level="DEBUG" />
        <Logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" />

        <!-- Database connection pool -->
        <Logger name="com.zaxxer.hikari" level="INFO" />

        <!-- Root logger -->
        <Root level="INFO">
            <AppenderRef ref="Console" />
            <AppenderRef ref="File" />
            <AppenderRef ref="ErrorFile" />
        </Root>
    </Loggers>
</Configuration>
